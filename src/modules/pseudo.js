//
// project obj
    // -name
    // -array of tasks
    // -description, optionally

    // methods:
    // -save
    // -set
    // -get
    // -get task
    // -add task
    // -remove task
    // -update tasklist

// task obj
    // -name
    // -due Date
    // -priority
    // -status (done or not?)

    // methods:
    // -set name
    // -get name
    // -set Date
    // -get Date
    // -set prior.
    // -get prior.
    // -set status
    // -get status

// Storage
// UI

